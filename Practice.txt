st.header("Our SEPM Project")
st.subheader('by Rohit Singh')

"""
# Twitter
## Analytic report

"""

dictionary = {
    "Leader" : "Rohit Singh",
    "Members" : "Meh"
}

list1 = [1,2,3]
st.write(dictionary) #printing
st.write(list1)
st.sidebar.title("Menu")
st.image("https://images-na.ssl-images-amazon.com/images/I/61xlC7PWspL._UX569_.jpg")""""

r = requests.get(f"https://api.stocktwits.com/api/2/streams/symbol/{symbol}.json") 
#f is used for editing link and {} is put where we have to change values.

symbol = st.sidebar.text_input('Symbol',value = 'AAPL', max_chars=5)
# taking input from user. default being AAPL

st.markdown("<h1 style='text-align: center; color: red;'>Some title</h1>", unsafe_allow_html=True)
